// src/app/app.routes.ts

import { Routes } from '@angular/router';

export const routes: Routes = [
  // Define your application routes here if you have any.
  // Example: { path: 'home', component: HomeComponent },
];